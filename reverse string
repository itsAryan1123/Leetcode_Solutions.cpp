class Solution {
public:
    vector<int> runningSum(vector<int>& nums) {
        for (int i = 1; i < nums.size(); i++) {
            // Each element becomes itself + the one before it
            nums[i] = nums[i] + nums[i-1];
        }
        return nums;
    }
};
